{
  "name": "stoic-meditations",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web run start",
    "build": "npm --prefix web run build && npm --prefix web run cap:sync",
    "install:all": "npm install && npm --prefix web install && npm --prefix functions install"
  }
}
