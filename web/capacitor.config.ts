import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.fluin.stoic',
  appName: 'Stoic Meditations',
  webDir: 'dist/stoic.fluin.io/browser'
};

export default config;
